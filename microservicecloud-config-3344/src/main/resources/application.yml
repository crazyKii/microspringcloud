server:
  port: 3344

spring:
  application:
    name: microservicecloud-config
  cloud:
    config:
      server:
        git:
          uri: git@github.com:crazyKii/microservicecloud-config.git # Github上git仓库名字